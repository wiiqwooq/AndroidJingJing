<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.week1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Week7">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Week6"></activity>
        <activity android:name=".Week5">
        </activity>
        <activity android:name=".Week4_Intents" />
        <activity android:name=".Week3C" />
        <activity android:name=".Week3B" />
        <activity android:name=".Week3" />
        <activity android:name=".UI" />
        <activity android:name=".MainActivity" />
    </application>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

</manifest>